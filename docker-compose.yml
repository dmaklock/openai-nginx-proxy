version: '3.8'

services:
  app:
    build: .
    container_name: openai-proxy
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - PORT=3000
      - OPENAI_API_URL=${OPENAI_API_URL:-https://api.openai.com}
